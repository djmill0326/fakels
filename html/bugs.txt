- folders aren't being checked against mime type properly when containing a '.', leading to them being detected as both a file and a folder. i actually have no idea. how is it subtitled 'Folder' yet using a nonexistent cover art (not even falling back to default somehow?!?!) with the click handler treating it as a file? oh ok so it's half folder, half non-media file. non-media files have no cover art defined, since they don't exist in library mode. what the fuck happened tho. could be solved in less than 5 minutes if I looked at the codebase in all likelihood. idgaf
- add search shorthand. it's literally two lines
- for the love of God just figure out how to disable the button tap highliggting mobile devices use. also apply pseudo :focus and :active to the seledted item's new position. fucking manually unstyle the original position's virtual shell temporarily to remove the active styling because it's insanely jarring once the scroll causes it to now be on the incorrect element
- i'll never catch this bug. somehow handleHold stops cancelling click events, unsure of cause. reloading fixes it, so clearly something has to get desynced
